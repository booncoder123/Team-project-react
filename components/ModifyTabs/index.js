import { styled } from "@mui/material/styles";
import Tabs from "@mui/material/Tabs";
const ModifyTabs = styled((props) => (
    <Tabs {...props} indicator={{height:20}} >
    </Tabs>
  ))(({}) => ({
    "&.MuiTabs-root": {
      
  
    },
   
   
  }));
  export default ModifyTabs;
  
  